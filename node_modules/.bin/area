#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gh-scripts/bin/gh-scripts.mjs" "$@"
  ret=$?
else 
  node  "$basedir/../gh-scripts/bin/gh-scripts.mjs" "$@"
  ret=$?
fi
exit $ret
